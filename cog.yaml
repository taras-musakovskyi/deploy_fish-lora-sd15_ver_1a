build:
  gpu: true
  cuda: "11.8"
  python_version: "3.11"

  system_packages:
    - "libgl1-mesa-glx"

  run:
    - "pip install torch==2.1.0"
    - "pip install diffusers==0.25.0 transformers==4.36.0 accelerate==0.25.0 peft==0.7.1"
    - "pip install huggingface-hub==0.20.0"

predict: "predict.py:Predictor"
