build:
  gpu: true
  cuda: "12.1"
  python_version: "3.11"

  system_packages:
    - "libgl1-mesa-glx"

  run:
    - "pip install torch==2.5.1 torchvision==0.20.1"
    - "pip install diffusers==0.31.0 transformers==4.46.3 accelerate==1.1.1"
    - "pip install peft==0.13.2 safetensors==0.4.5"

predict: "predict.py:Predictor"
